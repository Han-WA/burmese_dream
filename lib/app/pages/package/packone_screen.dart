import 'package:flutter/material.dart';

class PackOneScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Package ONE"),
      ),
    );
  }
}

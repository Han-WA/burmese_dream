import 'package:flutter/material.dart';

class PackTwoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Package TWO"),
      ),
    );
  }
}
